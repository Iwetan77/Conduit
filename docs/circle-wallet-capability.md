# Circle wallet capability

Generated by `scripts/circle-wallet-probe.ts` on 2026-08-30T21:41:30.161Z.
Every claim is followed by the call that established it. Nothing here is inferred
from documentation, and no response was edited.

- Base URL: `https://api.circle.com`
- Circle user: `probe-db0d7cad-f281-42ec-8943-9b16a518ca85` — created by this run, PIN mode, initialized through
  a real browser challenge.

## The question this was written to answer

**An already-initialized user can hold more than one wallet on Arc.**

- The wallet `/user/initialize` made: `161c8e40-598a-541b-a4c1-3b2e3a4ff1de` `0xc2b5368a57e4048073d74103257be809b3695683` accountType `EOA`
- Provisioned afterwards: `c05071d3-fd13-5eae-a668-f6f52eb6b1cc` `0x28c412b3996de24b845a45daeebfea59de5b509b` accountType `SCA` name `Conduit SCA probe`
- Provisioned afterwards: `9ffbdd50-7239-5883-817a-c08528b6fe84` `0x3bf577263b3ef424a7837c7148d62c8c3fff5a35` accountType `EOA` name `Conduit settlement`
- Arc wallets on this one user at the end of the run: **3**

The design's premise holds: a merchant can be given a settlement wallet that is
separate from the wallet they sign in with, owned by them, created through Circle.

## What else was established

- **The endpoint is `POST /v1/w3s/user/wallets`**, body `{idempotencyKey, blockchains, accountType?, metadata?}`.
- **It returns a `challengeId`, not a wallet** (2 issued here). Creation completes in the browser, on the user's device — there is no server-side completion, and an unexecuted challenge creates nothing.
- **The blockchain identifier is `ARC-TESTNET`**, read back from `ListWallets` — the same string the API server already sends to `/user/initialize`.
- **Arc supports both account types**: `SCA` and `EOA` were both created on this user. Account type is fixed at creation, so this is a decision to take once per wallet, not later.
- **Naming survives into `ListWallets`** (`c05071d3-fd13-5eae-a668-f6f52eb6b1cc` name `Conduit SCA probe`, refId `conduit-sca-probe`), so the business wallet is distinguishable from the personal one without keeping a mapping of our own. Worth having, but not worth trusting: the server should still record the wallet id it provisioned.
- **Wallet order in `ListWallets` is not stable.** The newest wallet came back first here. Nothing may identify a wallet by position.
- **Two wallets on one chain in a single request is rejected** (`400`, code `156017`: The specified blockchain parameters are incorrect.). One request per wallet.
- **`/user/initialize` is one-shot** — a second call answers `409` code `155106`: The user had already been initialized.. So a wallet cannot be folded into the initialize challenge for an existing user; first login is initialize, and every wallet after it is `/user/wallets`.
- **An SSO user's token cannot be minted from the API key.** `POST /v1/w3s/users/token` answers `code 2, API parameter invalid` for a user whose `authMode` is `SSO` — which every Conduit merchant is, since they sign in with Google. The server therefore cannot act on a merchant's Circle wallets on its own; it needs the user token the browser holds. That is why provisioning takes `X-Circle-User-Token` rather than being a background job.

## Wallets at the end of the run

| blockchain | accountType | state | address | name | refId |
| --- | --- | --- | --- | --- | --- |
| `ARC-TESTNET` | `SCA` | `LIVE` | `0x28c412b3996de24b845a45daeebfea59de5b509b` | Conduit SCA probe | conduit-sca-probe |
| `ARC-TESTNET` | `EOA` | `LIVE` | `0x3bf577263b3ef424a7837c7148d62c8c3fff5a35` | Conduit settlement | conduit-settlement |
| `ARC-TESTNET` | `EOA` | `LIVE` | `0xc2b5368a57e4048073d74103257be809b3695683` |  |  |

## Call log

Every request this run made, in order, with Circle's own response.

### create user

`POST /v1/w3s/users` → http 201

Request:

```json
{
  "userId": "probe-db0d7cad-f281-42ec-8943-9b16a518ca85"
}
```

Response:

```json
{
  "data": {
    "id": "probe-db0d7cad-f281-42ec-8943-9b16a518ca85",
    "status": "ENABLED",
    "createDate": "2026-08-30T21:39:53Z",
    "pinStatus": "UNSET",
    "pinDetails": {
      "failedAttempts": 0
    },
    "securityQuestionStatus": "UNSET",
    "securityQuestionDetails": {
      "failedAttempts": 0
    },
    "authMode": "PIN"
  }
}
```

### issue user token

`POST /v1/w3s/users/token` → http 200

Request:

```json
{
  "userId": "probe-db0d7cad-f281-42ec-8943-9b16a518ca85"
}
```

Response:

```json
{
  "data": {
    "userToken": "<redacted>",
    "encryptionKey": "<redacted>"
  }
}
```

### initialize

`POST /v1/w3s/user/initialize` → http 201

Request:

```json
{
  "idempotencyKey": "d646c211-005a-4e2a-a10b-2e1c515732eb",
  "blockchains": [
    "ARC-TESTNET"
  ]
}
```

Response:

```json
{
  "data": {
    "challengeId": "f4170040-36a5-5da4-a555-bbc2d46dfc6f"
  }
}
```

### wallets after initialize

`GET /v1/w3s/wallets` → http 200

Response:

```json
{
  "data": {
    "wallets": [
      {
        "id": "161c8e40-598a-541b-a4c1-3b2e3a4ff1de",
        "state": "LIVE",
        "walletSetId": "5f65ac8a-8d38-50a9-8c30-d1f27705a584",
        "custodyType": "ENDUSER",
        "userId": "probe-db0d7cad-f281-42ec-8943-9b16a518ca85",
        "address": "0xc2b5368a57e4048073d74103257be809b3695683",
        "blockchain": "ARC-TESTNET",
        "accountType": "EOA",
        "updateDate": "2026-08-30T21:40:50Z",
        "createDate": "2026-08-30T21:40:50Z"
      }
    ]
  }
}
```

### additional wallet

`POST /v1/w3s/user/wallets` → http 201

Request:

```json
{
  "idempotencyKey": "2e47b7bf-80c8-4d28-a96f-42e1d06b0ab4",
  "blockchains": [
    "ARC-TESTNET"
  ],
  "metadata": [
    {
      "name": "Conduit settlement",
      "refId": "conduit-settlement"
    }
  ]
}
```

Response:

```json
{
  "data": {
    "challengeId": "24d1369f-0248-5421-a010-5f5835ac7cbe"
  }
}
```

### additional wallet, accountType=SCA

`POST /v1/w3s/user/wallets` → http 201

Request:

```json
{
  "idempotencyKey": "9659cc38-bcd0-4759-a413-c5df9f6c6ace",
  "blockchains": [
    "ARC-TESTNET"
  ],
  "accountType": "SCA",
  "metadata": [
    {
      "name": "Conduit SCA probe",
      "refId": "conduit-sca-probe"
    }
  ]
}
```

Response:

```json
{
  "data": {
    "challengeId": "0140ccb4-590e-5691-bc53-74d05aa5a5c5"
  }
}
```

### two wallets on one chain in one request

`POST /v1/w3s/user/wallets` → http 400 · code 156017

Request:

```json
{
  "idempotencyKey": "8d535c77-bbd2-4f82-a8d6-384499fd0c3b",
  "blockchains": [
    "ARC-TESTNET",
    "ARC-TESTNET"
  ]
}
```

Response:

```json
{
  "code": 156017,
  "message": "The specified blockchain parameters are incorrect.",
  "errors": [
    {
      "invalidValue": "ARC-TESTNET",
      "message": "Duplicate blockchain in request"
    }
  ]
}
```

### initialize again

`POST /v1/w3s/user/initialize` → http 409 · code 155106

Request:

```json
{
  "idempotencyKey": "0113ec3b-b941-44d1-aad6-976a98919e11",
  "blockchains": [
    "ARC-TESTNET"
  ]
}
```

Response:

```json
{
  "code": 155106,
  "message": "The user had already been initialized."
}
```

### wallets at the end

`GET /v1/w3s/wallets` → http 200

Response:

```json
{
  "data": {
    "wallets": [
      {
        "id": "c05071d3-fd13-5eae-a668-f6f52eb6b1cc",
        "state": "LIVE",
        "walletSetId": "5f65ac8a-8d38-50a9-8c30-d1f27705a584",
        "custodyType": "ENDUSER",
        "userId": "probe-db0d7cad-f281-42ec-8943-9b16a518ca85",
        "refId": "conduit-sca-probe",
        "name": "Conduit SCA probe",
        "address": "0x28c412b3996de24b845a45daeebfea59de5b509b",
        "blockchain": "ARC-TESTNET",
        "accountType": "SCA",
        "updateDate": "2026-08-30T21:41:21Z",
        "createDate": "2026-08-30T21:41:21Z",
        "scaCore": "circle_6900_singleowner_v3"
      },
      {
        "id": "9ffbdd50-7239-5883-817a-c08528b6fe84",
        "state": "LIVE",
        "walletSetId": "5f65ac8a-8d38-50a9-8c30-d1f27705a584",
        "custodyType": "ENDUSER",
        "userId": "probe-db0d7cad-f281-42ec-8943-9b16a518ca85",
        "refId": "conduit-settlement",
        "name": "Conduit settlement",
        "address": "0x3bf577263b3ef424a7837c7148d62c8c3fff5a35",
        "blockchain": "ARC-TESTNET",
        "accountType": "EOA",
        "updateDate": "2026-08-30T21:41:11Z",
        "createDate": "2026-08-30T21:41:11Z"
      },
      {
        "id": "161c8e40-598a-541b-a4c1-3b2e3a4ff1de",
        "state": "LIVE",
        "walletSetId": "5f65ac8a-8d38-50a9-8c30-d1f27705a584",
        "custodyType": "ENDUSER",
        "userId": "probe-db0d7cad-f281-42ec-8943-9b16a518ca85",
        "address": "0xc2b5368a57e4048073d74103257be809b3695683",
        "blockchain": "ARC-TESTNET",
        "accountType": "EOA",
        "updateDate": "2026-08-30T21:40:50Z",
        "createDate": "2026-08-30T21:40:50Z"
      }
    ]
  }
}
```
