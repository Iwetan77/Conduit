{
  "name": "@conduit/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "forge build",
    "test": "forge test --fork-url https://rpc.testnet.arc.network -vvv",
    "deploy": "forge script script/Deploy.s.sol --rpc-url arc_testnet --broadcast --slow",
    "clean": "forge clean"
  },
  "dependencies": {
    "ethers": "^6.11.0"
  }
}
