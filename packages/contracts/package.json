{
  "name": "@conduit/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "test:fork": "forge test --fork-url ${ARC_TESTNET_RPC:-https://rpc.testnet.arc.network} -vvv",
    "deploy": "forge script script/Deploy.s.sol --rpc-url arc_testnet --broadcast --slow",
    "clean": "forge clean",
    "deploy:payroll": "forge script script/DeployPayroll.s.sol --rpc-url arc_testnet --broadcast"
  },
  "dependencies": {
    "ethers": "^6.11.0"
  },
  "license": "SEE LICENSE IN LICENSE"
}
